# SFTP Manager Menu Structure

> 메뉴 및 네비게이션 구조 정의

## Application Menu (Title Bar)

```
File    Edit    View    Server    Help
```

### File Menu
```
File
├── New Server...          Ctrl+N
├── Import Config...
├── Export Config...
├── ─────────────────────
├── Settings               Ctrl+,
├── ─────────────────────
└── Exit                   Alt+F4
```

### Edit Menu
```
Edit
├── Copy Connection Info   Ctrl+C
├── Copy Password
├── Copy SFTP Command
└── Copy FileZilla URL
```

### View Menu
```
View
├── Dashboard              Ctrl+1
├── Server List            Ctrl+2
├── Settings               Ctrl+3
├── ─────────────────────
├── Refresh                F5
├── ─────────────────────
├── Toggle Sidebar         Ctrl+B
└── Toggle Dark Mode       Ctrl+D
```

### Server Menu
```
Server
├── Start                  F6
├── Stop                   F7
├── Restart                F8
├── ─────────────────────
├── Start All              Ctrl+Shift+S
├── Stop All               Ctrl+Shift+X
├── ─────────────────────
├── View Logs              Ctrl+L
├── ─────────────────────
└── Remove...              Del
```

### Help Menu
```
Help
├── Documentation          F1
├── Keyboard Shortcuts     Ctrl+/
├── ─────────────────────
├── Check for Updates
├── ─────────────────────
└── About SFTP Manager
```

---

## Sidebar Navigation

```
┌─────────────────────┐
│  SFTP Manager       │
├─────────────────────┤
│  📊 Dashboard       │  ← Home/Overview
│  📋 Servers         │  ← Server List
│  ⚙️  Settings       │  ← App Settings
├─────────────────────┤
│  Docker: ● Online   │  ← Status indicator
└─────────────────────┘
```

---

## Context Menus

### Server Card Context Menu (Right-click)

```
┌─────────────────────┐
│ ▶ Start            │  (if stopped)
│ ⏹ Stop             │  (if running)
│ 🔄 Restart         │
├─────────────────────┤
│ 📋 Copy Info       ▶│──┬─ Full Info
│                     │  ├─ SFTP Command
│                     │  ├─ FileZilla URL
│                     │  └─ Password Only
├─────────────────────┤
│ 📝 View Logs       │
│ ✏️ Edit            │
├─────────────────────┤
│ 🗑️ Remove...       │
└─────────────────────┘
```

### Tray Icon Menu (System Tray)

```
┌─────────────────────┐
│ Open SFTP Manager  │
├─────────────────────┤
│ Servers            ▶│──┬─ sftp-main (●)
│                     │  ├─ sftp-backup (○)
│                     │  └─ ...
├─────────────────────┤
│ Start All          │
│ Stop All           │
├─────────────────────┤
│ Exit               │
└─────────────────────┘
```

---

## Keyboard Shortcuts

### Global

| Shortcut | Action |
|----------|--------|
| `Ctrl+N` | New Server |
| `Ctrl+,` | Settings |
| `Ctrl+Q` | Quit |
| `F5` | Refresh |
| `Ctrl+D` | Toggle Dark Mode |
| `Ctrl+B` | Toggle Sidebar |
| `Ctrl+/` | Show Shortcuts |

### Server Management

| Shortcut | Action |
|----------|--------|
| `F6` | Start Selected |
| `F7` | Stop Selected |
| `F8` | Restart Selected |
| `Ctrl+Shift+S` | Start All |
| `Ctrl+Shift+X` | Stop All |
| `Del` | Remove Selected |
| `Ctrl+L` | View Logs |

### Copy Actions

| Shortcut | Action |
|----------|--------|
| `Ctrl+C` | Copy Full Info |
| `Ctrl+Shift+C` | Copy SFTP Command |
| `Ctrl+Alt+C` | Copy Password |

### Navigation

| Shortcut | Action |
|----------|--------|
| `Ctrl+1` | Go to Dashboard |
| `Ctrl+2` | Go to Servers |
| `Ctrl+3` | Go to Settings |
| `↑/↓` | Navigate Servers |
| `Enter` | Select/Open Server |
| `Esc` | Close Modal/Deselect |

---

## Modal Dialogs

### Confirmation Dialog (Remove Server)

```
┌────────────────────────────────────────┐
│  ⚠️ Remove Server                      │
├────────────────────────────────────────┤
│                                        │
│  Are you sure you want to remove       │
│  "sftp-main"?                          │
│                                        │
│  This will:                            │
│  • Stop the container                  │
│  • Remove the container                │
│  • Delete saved configuration          │
│                                        │
│  ⚠️ This action cannot be undone.      │
│                                        │
│           [Cancel]  [Remove]           │
└────────────────────────────────────────┘
```

### Toast Notifications

```
┌────────────────────────────────────┐
│ ✓ Server "sftp-main" started      │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ ✓ Connection info copied          │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ ✗ Failed to start server          │
│   Docker is not running           │
└────────────────────────────────────┘
```
